# docker/

Reserved for container assets that don't belong in the top-level
`Dockerfile` — e.g. an Nginx config if the optional dashboard (roadmap
Phase 11) is added, or custom entrypoint scripts. Empty for now: the API's
`Dockerfile` and `docker-compose.yml` live at the repository root.
